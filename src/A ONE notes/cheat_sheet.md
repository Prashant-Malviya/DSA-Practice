# Compile
```java
javac -d out src/utils/*.java src/matrix/*.java
```

# Run
```java
java -cp out matrix.P02_boundary_traversal
```

### 🧠 THE CORE IDEA (remember forever)

- Compile once → run many times

- Java is a compiled language, not interpreted.

### to open preview for .md file

```java
    Ctrl + Shift + V
```


----

